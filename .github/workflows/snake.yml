name: Generate Snake

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        
      - name: Generate Snake
        uses: Platane/snk@v3
        with:
          github_user_name: shahrufhasan
          outputs: |
            dist/snake.svg
            dist/snake-dark.svg?palette=github-dark
            
      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

## **Step 6: Commit the file**

1. Scroll down
2. Click **"Commit changes"** button (green)
3. Click **"Commit changes"** again in the popup

---

## **Step 7: Go to Actions tab**

1. Click on **"Actions"** tab at the top of your repo

---

## **Step 8: Run the workflow**

1. On the left sidebar, click **"Generate Snake"**
2. You'll see a message: "This workflow has a workflow_dispatch event trigger"
3. Click **"Run workflow"** dropdown (blue button on right)
4. Click **"Run workflow"** again

---

## **Step 9: Wait for it to finish**

1. You'll see a yellow dot (running)
2. Wait 1-2 minutes
3. It should turn into a green checkmark ✅

---

## **Step 10: Check if it worked**

Open this link in your browser:
```
https://raw.githubusercontent.com/shahrufhasan/shahrufhasan/output/snake.svg
